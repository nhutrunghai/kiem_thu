{
  "info": {
    "name": "UniFlow API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:5050"
    },
    {
      "key": "token",
      "value": ""
    },
    {
      "key": "email",
      "value": "student@example.com"
    },
    {
      "key": "password",
      "value": "strongpass123"
    },
    {
      "key": "fullName",
      "value": "Student One"
    },
    {
      "key": "eventId",
      "value": ""
    },
    {
      "key": "taskId",
      "value": ""
    },
    {
      "key": "noteId",
      "value": ""
    },
    {
      "key": "notificationId",
      "value": ""
    },
    {
      "key": "resetToken",
      "value": ""
    },
    {
      "key": "newPassword",
      "value": "newstrongpass123"
    }
  ],
  "item": [
    {
      "name": "Auth",
      "item": [
        {
          "name": "Register",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/register",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "register"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}",
                  "try {",
                  "  const json = pm.response.json();",
                  "  if (json && json.token) {",
                  "    pm.collectionVariables.set('token', json.token);",
                  "    pm.collectionVariables.set('validToken', json.token);",
                  "  }",
                  "  if (json && json.user && json.user.email) {",
                  "    pm.collectionVariables.set('email', json.user.email);",
                  "    pm.collectionVariables.set('validEmail', json.user.email);",
                  "  }",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "Login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}",
                  "try {",
                  "  const json = pm.response.json();",
                  "  if (json && json.token) {",
                  "    pm.collectionVariables.set('token', json.token);",
                  "    pm.collectionVariables.set('validToken', json.token);",
                  "  }",
                  "  if (json && json.user && json.user.email) {",
                  "    pm.collectionVariables.set('email', json.user.email);",
                  "    pm.collectionVariables.set('validEmail', json.user.email);",
                  "  }",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "Forgot Password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/forgot-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "forgot-password"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Reset Password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/reset-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "reset-password"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Users",
      "item": [
        {
          "name": "Get Profile",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/user/profile",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "user",
                "profile"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Update Profile",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/user/profile",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "user",
                "profile"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Events",
      "item": [
        {
          "name": "List Events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/events",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "events"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Create Event",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/events",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "events"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}",
                  "try {",
                  "  const json = pm.response.json();",
                  "  const id = json && (json._id || json.id);",
                  "  if (id) {",
                  "    pm.collectionVariables.set('eventId', id);",
                  "    if (!pm.collectionVariables.get('validEventId')) {",
                  "      pm.collectionVariables.set('validEventId', id);",
                  "    } else if (!pm.collectionVariables.get('secondaryEventId') && pm.collectionVariables.get('validEventId') !== id) {",
                  "      pm.collectionVariables.set('secondaryEventId', id);",
                  "    }",
                  "  }",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "Update Event",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/events/{{eventId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "events",
                "{{eventId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Delete Event",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/events/{{eventId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "events",
                "{{eventId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Tasks",
      "item": [
        {
          "name": "List Tasks",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/tasks",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "tasks"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Create Task",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/tasks",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "tasks"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}",
                  "try {",
                  "  const json = pm.response.json();",
                  "  const id = json && (json._id || json.id);",
                  "  if (id) {",
                  "    pm.collectionVariables.set('taskId', id);",
                  "    if (!pm.collectionVariables.get('validTaskId')) {",
                  "      pm.collectionVariables.set('validTaskId', id);",
                  "    }",
                  "  }",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "Update Task",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/tasks/{{taskId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "tasks",
                "{{taskId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Delete Task",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/tasks/{{taskId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "tasks",
                "{{taskId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Notes",
      "item": [
        {
          "name": "List Notes",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/notes",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notes"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Get Note by Event",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/notes/{{eventId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notes",
                "{{eventId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert Note",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/notes",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notes"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}",
                  "try {",
                  "  const json = pm.response.json();",
                  "  const id = json && (json._id || json.id);",
                  "  if (id) {",
                  "    pm.collectionVariables.set('noteId', id);",
                  "    if (!pm.collectionVariables.get('validNoteId')) {",
                  "      pm.collectionVariables.set('validNoteId', id);",
                  "    }",
                  "  }",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "Disable Note Reminder",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/notes/{{noteId}}/reminder",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notes",
                "{{noteId}}",
                "reminder"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Delete Note",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/notes/{{noteId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notes",
                "{{noteId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Notifications",
      "item": [
        {
          "name": "List Notifications",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/notifications",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notifications"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}",
                  "try {",
                  "  const json = pm.response.json();",
                  "  if (Array.isArray(json) && json.length) {",
                  "    const id = json[0]._id || json[0].id;",
                  "    if (id) {",
                  "      pm.collectionVariables.set('notificationId', id);",
                  "      if (!pm.collectionVariables.get('validNotificationId')) {",
                  "        pm.collectionVariables.set('validNotificationId', id);",
                  "      }",
                  "    }",
                  "  }",
                  "} catch (e) {}"
                ]
              }
            }
          ]
        },
        {
          "name": "Update Notification",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/notifications/{{notificationId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notifications",
                "{{notificationId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Dashboard",
      "item": [
        {
          "name": "Get Stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/dashboard/stats",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "dashboard",
                "stats"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Docs",
      "item": [
        {
          "name": "Get Swagger YAML",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/api/docs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "docs"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
                  "const enabled = data.enabled;",
                  "if (enabled !== undefined && enabled !== '' && String(enabled) !== '1') {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const target = data.request;",
                  "if (target && target !== pm.info.requestName) {",
                  "  pm.execution.skipRequest();",
                  "}",
                  "const body = data.body;",
                  "if (body !== undefined && body !== '') {",
                  "  const resolved = pm.variables.replaceIn(body);",
                  "  if (pm.request.body && pm.request.body.mode === 'raw') {",
                  "    pm.request.body.raw = resolved;",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const expectedStatus = pm.iterationData.get('expectedStatus');",
                  "if (expectedStatus !== undefined && expectedStatus !== '') {",
                  "  pm.test(`Status ${expectedStatus}`, function () {",
                  "    pm.response.to.have.status(parseInt(expectedStatus, 10));",
                  "  });",
                  "}",
                  "const expectedMessage = pm.iterationData.get('expectedMessage');",
                  "if (expectedMessage) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message equals', function () {",
                  "    pm.expect(json && json.message).to.eql(expectedMessage);",
                  "  });",
                  "}",
                  "const expectedMessageContains = pm.iterationData.get('expectedMessageContains');",
                  "if (expectedMessageContains) {",
                  "  let json = {};",
                  "  try { json = pm.response.json(); } catch (e) {}",
                  "  pm.test('Message contains', function () {",
                  "    pm.expect(json && json.message).to.be.a('string');",
                  "    pm.expect(json.message).to.include(expectedMessageContains);",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "const data = pm.iterationData ? pm.iterationData.toObject() : {};",
          "const resetRun = data.resetRun;",
          "const target = data.request;",
          "if (resetRun === '1' && target && target === pm.info.requestName) {",
          "  const resetKeys = ['validEventId','secondaryEventId','validTaskId','validNoteId','validNotificationId','validToken','validEmail','eventId','taskId','noteId','notificationId'];",
          "  for (const k of resetKeys) { pm.collectionVariables.unset(k); }",
          "}",
          "const upperFirst = (s) => s ? s.charAt(0).toUpperCase() + s.slice(1) : s;",
          "const resolveMacro = (key, value) => {",
          "  if (value === '__EMPTY__') return '';",
          "  if (value === '__INVALID__') {",
          "    if (key === 'token') return 'invalid.token.value';",
          "    return '123';",
          "  }",
          "  if (value === '__VALID__') {",
          "    const saved = pm.collectionVariables.get('valid' + upperFirst(key));",
          "    return saved !== undefined && saved !== null ? saved : value;",
          "  }",
          "  if (value === '__SECONDARY__') {",
          "    const saved = pm.collectionVariables.get('secondary' + upperFirst(key));",
          "    return saved !== undefined && saved !== null ? saved : value;",
          "  }",
          "  if (value === '__LAST__') {",
          "    const saved = pm.collectionVariables.get(key);",
          "    return saved !== undefined && saved !== null ? saved : value;",
          "  }",
          "  return value;",
          "};",
          "const setVar = (key, value) => {",
          "  if (value === undefined) return;",
          "  pm.variables.set(key, value);",
          "  pm.collectionVariables.set(key, value);",
          "};",
          "const keys = ['baseUrl','email','password','fullName','token','resetToken','newPassword','eventId','taskId','noteId','notificationId','resetRun','request'];",
          "for (const key of keys) {",
          "  if (!Object.prototype.hasOwnProperty.call(data, key)) continue;",
          "  const raw = data[key];",
          "  if (typeof raw === 'string' && raw.trim() === '' && raw !== '__EMPTY__') continue;",
          "  let value = raw;",
          "  if (value === 'RANDOM' && key === 'email') {",
          "    const rand = Math.floor(Math.random() * 100000);",
          "    value = `student+${Date.now()}${rand}@example.com`;",
          "  }",
          "  value = resolveMacro(key, value);",
          "  setVar(key, value);",
          "}"
        ]
      }
    }
  ]
}